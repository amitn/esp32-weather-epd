[project]
name = "esp32-weather-epd"
version = "0.1.0"
description = "ESP32 E-Paper Weather Display - Python build tools and dependencies"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "cryptography>=41.0.0",
    "Pillow>=10.0.0",
    "platformio>=6.1.0",
]
